// Archivo Header.js para el proyecto Biospace

import React from 'react';

// Componente de encabezado
const Header = () => {
    return (
        <header className="bg-dark text-white text-center py-4">
            <h1>Biospace</h1>
            <p>Your gateway to visualizing biological experiments in space</p>
        </header>
    );
};

export default Header;